{
  "name": "lembots",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run preview -- --host 0.0.0.0 --port ${PORT:-4173}"
  },
  "engines": {
    "node": ">=18"
  }
}
