# Agent instructions

## Build plan adherence
- Follow the step-by-step build plan in `README.md` ("Codex build plan").
- When you complete a step, update the checklist item from `[ ]` to `[x]` in `README.md`.
- Keep checklist updates in the same commit as the work that completes the step.
